# fastly_gzip

Creates and updates a Fastly gzip configuration.

## Actions

* `:create`
* `:update`

## Properties

* `api_key`, `username`, `password` - Fastly authentication.
* `service` - Fastly service name.
* `extensions`, `content_types`, `cache_condition` - Gzip settings.
